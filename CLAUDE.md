# Codestyle

- Use barrel files
- Always use curly braces in conditional statements
- For every model create a zod schema in separate file
- Always import zod like this `import * as z from 'zod';`

# Angular

- Use signals
- Use resources
- Use signal forms
